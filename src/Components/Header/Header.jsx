import Navbar from "./Navbar";


const Header = () => {
    return (
        <header className="bg-[var(--primary)] text-[var(--secondary)] border-b-2">
            <Navbar></Navbar>
        </header>
    );
};

export default Header;